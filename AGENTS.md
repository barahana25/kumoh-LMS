# 작업 재개 안내

- 이 프로젝트를 이어서 작업할 때 `docs/HANDOFF.md`를 먼저 읽는다. Claude의 중단 지점, 현재 검증 결과, 남은 구현 순서를 정리한 문서다.
- 설계와 구현 계획은 `docs/superpowers/`에 있다. 계획의 예제보다 실제 코드와 회귀 테스트를 우선한다. 계획 작성 뒤 수정된 버그가 있다.
- 기존 미커밋 파일을 진행 중인 작업으로 취급하고, 삭제하거나 덮어쓰기 전에 내용을 확인한다.
- 로컬 자격증명 파일 `env`를 출력하거나 커밋하지 않는다. `sqlite3.dll`도 커밋 대상이 아니다.
- Windows PowerShell의 Flutter 경로는 `C:\src\flutter\bin\flutter.bat`이다. 검증 명령은 인수인계 문서에 있다.
