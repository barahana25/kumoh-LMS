# ---- Secrets (NEVER commit) ----
env
.env
*.env
**/secrets.dart
*.jks
*.keystore
key.properties

# ---- Flutter / Dart ----
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# (pubspec.lock is intentionally committed — apps pin their resolved versions)
*.g.dart.bak

# ---- IDE / OS ----
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# ---- Android / iOS build ----
android/.gradle/
android/app/debug/
android/app/profile/
android/app/release/
# iOS는 배포하지 않는다. 로컬에서만 쓰고 저장소에는 올리지 않는다.
ios/

# SDD scratch
.superpowers/

# Windows sqlite3 native lib for `flutter test` (Drift)
sqlite3.dll

# Flutter tool crash logs
flutter_*.log

# 데스크톱/에뮬레이터 실행 중 작업 폴더에 떨어지는 암호화 앱 DB
kumoh_lms.sqlite

# SSO 우회 재현·진단 스크립트 (공개 저장소 커밋 금지)
python/

# 스파이크 의존성
tool/spike/node_modules/

relay/node_modules/

# Google Play 배포용 서비스 계정 키. 절대 커밋하지 않는다.
*.iam.gserviceaccount.json
stalwart-realm-*.json
play-deploy*.json
